"""Outsourcing-ceiling sheet modules, relocated from workbook_outsourcing_ceiling.

These already span all three programs (Virginia / Columbia / DDG-51) as columns.
Plain module namespace; the master sheet registry assembles the SheetEntry
objects in group order. Data binding lives in ``_bind.py``.
"""
