"""Submarine (Virginia + Columbia) sheet modules, relocated from workbook_submarines.

This is a plain module namespace; the master sheet registry
(workbook_master_tam/sheets/__init__.py) imports the individual SheetEntry
objects from these modules and assembles them in group order. Data binding for
these modules lives in ``_bind.py``.
"""
