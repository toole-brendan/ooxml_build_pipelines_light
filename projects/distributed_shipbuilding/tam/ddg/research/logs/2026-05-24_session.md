# Session log — 2026-05-24

Session covered overnight data pulls + analysis of subaward coverage. Started
roughly 2026-05-23 ~22:30 (user kicking off overnight work before sleep) and
continued through 2026-05-24 morning analysis.

## TL;DR

- **USAspending sub-award pull**: DONE. 87 PIIDs, 26,811 records, $20.61B. No
  new PIID coverage vs. SAM, but 1.33x dollar capture — though 93% of that
  delta is one Raytheon ESSM PIID containing a $4.2B Thales NL NATO
  cooperative-development cost-share, not a real DDG-cost gap. Real net new
  coverage from USAspending: ~$350M–$1.1B legitimate.
- **Earnings transcript scrapers** for LMT/RTX/NOC/LHX/BAE: DONE via background
  agent. 89 new transcripts (134 in unified master).
- **FPDS capped-slug recovery pull**: STILL RUNNING (PID 53800, started
  08:36:48) writing to `fpds_raw_v2/`. Targets `gd_biw_navy` +
  `rolls_royce_navy_ddg`. Shell window was closed but `nohup` kept the process
  alive — verified PID still ticking. Currently at BIW q2 depth-4 splits.
- **Originals never touched**: `fpds_raw/`, `sam_subawards/`, and
  `pull_fpds_ddg_primes.py` all bit-for-bit identical to session start.

## What we started with (state at session start)

- FPDS pulls done into `fpds_raw/` for 13 slugs. Several capped:
  - `gd_biw_navy` q1 (BATH IRON WORKS): 3,000/7,990 est. records (capped)
  - `gd_biw_navy` q2 (GENERAL DYNAMICS BATH IRON WORKS): 3,000/31,240 est. (capped)
  - `rolls_royce_navy_ddg` q1 + q2: 3,000/3,590 est. each (capped, modest loss)
- SAM.gov sub-awards pulled for 89 PIIDs into `sam_subawards/`: 24,559 records, $15.50B
- NAICS resolved for top-150 vendors (97 OK, 53 not-found) → `extracted/entity_naics_lookup.csv`
- GD + HII earnings transcripts in `gd_earnings_transcripts/` (22) + `hii_earnings_transcripts/` (23)
- `usaspending_subawards/` directory was empty
- `nc_scope_summary.json` denominator: $13.84B in-scope DDG subaward $, 1,954 unique parent UEIs

## Chronological actions

### 1. Read overnight logs (`pull_logs/{fpds_pull,naics_pull,sam_pull}.log`)

Summarized the three to user. Flagged the FPDS cap-truncation risk on BIW /
Rolls-Royce / BAE-Land.

### 2. User picked overnight work: A (USAspending) + C (transcripts)

Other options offered but skipped: B (re-pull capped FPDS — done later in
session); D (combine A+B).

### 3. Launched USAspending pull (background)

Command: `python3 scripts/pull_usaspending_subawards.py --discover`
- Discovered 87 candidate DDG PIIDs from FPDS raw data ($50M+ floor, top-30/yard, top-15/other-group)
- 2-call pattern per PIID: search → subawards endpoint
- USAspending.gov API is unauthenticated — no SAM_API_KEY used
- Completed 01:13:43 the next morning: 87/87 PIIDs resolved (0 not-found), 26,811 records, $20.61B
- Output: `usaspending_subawards/{PIID}_subawards.json` + `_summary.json` + `_seed_lookup.json` + `_discovered_piids.json`

### 4. Launched transcript-scraper agent (background subagent)

Generalized `scrape_gd_transcripts.py` pattern across 5 more primes: LMT, RTX,
NOC, LHX, BAE. Agent:
- Discovered URLs (Motley Fool primary; LHX used official L3Harris PDFs via q4cdn; BAE only on Insider Monkey)
- Created `scripts/scrape_transcripts_generic.py` + `scripts/transcripts_url_config.json` + `scripts/build_transcripts_master_index.py` + `scripts/write_transcripts_summary.py`
- Ran scrapes; 89 new transcripts saved across 5 per-ticker dirs
- Built `extracted/all_transcripts_index.csv` — 134-row master across all 7 primes (GD+HII+5 new)
- Wrote `logs/transcripts_pull_summary.md` morning-read artifact

Per-ticker coverage:
- LMT: 21 transcripts (gaps Q1 2022, Q1 2023)
- RTX: 21 (no pre-2020-04-03 — merger date)
- NOC: 23 (full FY2020 Q1 → Q1 2026)
- LHX: 21 (mixed sources, gaps Q2/Q3 2020, Q2/Q3 2022)
- BAE: 3 (semi-annual reporter, no Motley Fool coverage, only 2024-2025 surfaced)

⚠️ **Caveat in agent's summary**: "Top DDG-relevant transcripts" ranking is
inflated by generic keywords. Spot-checked NOC FY2020 Q3 (agent claimed 11
mentions): hard DDG keywords (`DDG`, `destroyer`, `Arleigh`, `SEWIP`, `SLQ-32`)
= 0 each. The 11 came from `Mission Systems`=21 + `Navy`=5 + `electronic
warfare`=1 + `backlog`=15. The per-keyword `program_mention_counts` in each
`.meta.json` is correct, but the top-3 summary ranking leans on noisy
keywords. For real DDG-relevance ranking, re-score on narrow subset
(`DDG|destroyer|Arleigh|SEWIP|SLQ-32|Mk 41|Mk 45|SPY-6|Aegis|Phalanx|CIWS`).

### 5. User question: did USAspending use the SAM API key?

Confirmed NO. `pull_usaspending_subawards.py` has zero auth references (grepped
for auth|api_key|SAM_API|x-api|bearer|token — none found). USAspending.gov API
is public/unauthenticated. Only `pull_sam_subawards.py` and
`pull_sam_entity_naics.py` use the SAM_API_KEY.

### 6. User question: did % of total DDG cost go up?

**Short answer: marginally, not meaningfully.**

Headline numbers:
- SAM: 24,559 records / $15.50B / 87 in-scope PIIDs
- USAspending: 26,811 records / $20.61B / **same 87 PIIDs** (zero PIID expansion)
- Delta: +$5.10B (1.33x SAM)

Per-PIID delta breakdown:
- 84/87 PIIDs match within ±$100M (same FFATA filings surfacing in both)
- 3 PIIDs where USA caught more, dominated by ONE:
  - `N0002415C5420` Raytheon ESSM Block 2 EMD: **+$4,749.8M** — a single
    $4,201M Thales Nederland B.V. record dated 2015-03-11. That's the ESSM
    NATO cooperative-development cost share (10 partner nations), not US Navy
    DDG spend. USAspending unrolls it; SAM filters it out. Plus 198 duplicate
    (recipient, amount) pairs in USA (max 10x repeats) inflating count.
  - `N0002413C2307` HII FY17 DDG 51: +$803.4M (plausibly real)
  - `N0002411C4408` BAE C450: +$315.5M (plausibly real)
- 2 PIIDs where SAM caught more (HII construction, $370M + $221M deltas — SAM probably cleaner on dedup)

**Real net new from USAspending after backing out the ESSM artifact: ~$350M–$1.1B.**

Updated `extracted/sam_vs_usaspending_per_piid.csv` by re-running
`aggregate_sam_subawards.py`. (Previous version had USA columns all zero
because USAspending dir was empty at the time.)

Conclusion: USAspending is mostly a corroboration source. The big lever for
moving "% of DDG total cost" is fixing the FPDS truncation that left
potentially hundreds of DDG PIIDs out of the discovery pool entirely.

### 7. FPDS capped-slug recovery — implementation

Initial approach (rejected): edit `pull_fpds_ddg_primes.py` in-place. Made 2
benign edits (added `datetime` import; split `DATE_WINDOW` into
`DATE_START`/`DATE_END`). User interrupted with safety concern.

**User chose: write to `fpds_raw_v2/`, never touch original.** Reverted the
2 in-place edits — `pull_fpds_ddg_primes.py` now bit-for-bit identical to
session start. Verified via grep on `DATE_WINDOW` constant + missing
`from datetime` import line.

Created new standalone `scripts/pull_fpds_capped_recovery.py`:
- Only targets 2 capped slugs (`gd_biw_navy`, `rolls_royce_navy_ddg`)
- Recursive date-window bisection: if `total_pages` > 300, split SIGNED_DATE
  in half and recurse on each half. Max depth 6 (≤64 leaves)
- Output: `fpds_raw_v2/{slug}_raw.json` + `_summary.json` + `_pull_log.txt`
- Records the split tree in the output JSON for diagnostics

Launched at 08:36:48 as PID 53800 via nohup. User closed shell shortly
after — but verified process is still alive (nohup detaches from
controlling terminal). At time of session log write, process is at BIW q2
depth-4 splits (window 2020/08/23 to 2021/03/21, 200+ pages pulled).

### 8. Wrote this session log

User request: capture the session before context is lost.

## Artifacts produced this session

### New files
- `scripts/scrape_transcripts_generic.py` — parameterized scraper (fool / insidermonkey / pdf)
- `scripts/transcripts_url_config.json` — URL lists per ticker
- `scripts/build_transcripts_master_index.py` — builds unified master index
- `scripts/write_transcripts_summary.py` — writes morning-read summary
- `scripts/pull_fpds_capped_recovery.py` — date-bisection recovery pull (standalone)
- `extracted/all_transcripts_index.csv` — 134-row master across 7 primes
- `usaspending_subawards/*.json` — 87 PIIDs (+ 3 index/summary files)
- `{lmt,rtx,noc,lhx,bae}_earnings_transcripts/*.{txt,meta.json}` + `_index.csv` per ticker
- `logs/transcripts_pull_summary.md`
- `pull_logs/usaspending_pull.log`
- `pull_logs/{lmt,rtx,noc,lhx,bae}_transcripts.log`
- `pull_logs/fpds_recovery.log` (in progress)
- `logs/2026-05-24_session.md` (this file)
- `fpds_raw_v2/_pull_log.txt` (in progress; raw JSONs will land after recovery completes)

### Updated files (downstream re-aggregation triggered)
- `extracted/sam_vs_usaspending_per_piid.csv` — refreshed with new USA totals
- `extracted/sam_subaward_annual_by_prime.csv`
- `extracted/sam_subaward_top_parents.csv`
- `extracted/sam_subaward_full_dedup_check.txt`

### Files NOT touched (verified)
- `fpds_raw/*` — all 13 slug files identical to session start
- `pull_fpds_ddg_primes.py` — reverted, identical to session start
- `sam_subawards/*` — untouched
- `gd_earnings_transcripts/*`, `hii_earnings_transcripts/*` — untouched
- `.env` — untouched

## Key numbers to remember

- **SAM subaward $ in scope**: $13.84B (1,954 parents) — original `nc_scope_summary.json` figure
- **SAM total across all 89 pulled PIIDs**: $15.50B / 24,559 records
- **USAspending total across same 87 PIIDs**: $20.61B / 26,811 records
- **Realistic post-USA pool after de-artifact**: ~$14.2–14.9B (1–8% bump vs SAM-only)
- **Headline-naive pool if trusting USA blindly**: $18.9B — don't use; mostly Thales artifact

Top 5 SAM parent recipients (from refreshed `sam_subaward_top_parents.csv`):
1. DRS TRAINING & CONTROL SYSTEMS, LLC — $2,024.0M (557 subs)
2. ARCTIC SLOPE REGIONAL CORPORATION — $998.5M (404 subs)
3. MAINE MACHINE PRODUCTS COMPANY — $855.1M (56 subs)
4. IVECO DEFENCE VEHICLES S.P.A. — $707.1M (152 subs)
5. GENERAL DYNAMICS INFORMATION TECHNOLOGY — $480.9M (305 subs)

## Open items / what's left

### Live process
- **PID 53800** still running. Will write to `fpds_raw_v2/{gd_biw_navy,rolls_royce_navy_ddg}_raw.json` + `_summary.json` when done. ETA from initial estimate was 45-60 min total; been running ~45 min as of this writeup. Check progress with `tail pull_logs/fpds_recovery.log` and `ls -la fpds_raw_v2/`.
- To verify completion: `cat fpds_raw_v2/_summary.json` will exist when done.
- To kill if needed: `kill 53800` (or check PID with `cat logs/fpds_recovery.pid`).

### Next logical steps (NOT done this session)
1. Once `fpds_raw_v2/` recovery completes, diff record counts vs `fpds_raw/` to quantify the recovery. Expected: BIW jumps from ~5,692 records to ~25-35K; Rolls-Royce from ~2,979 to ~5-6K.
2. If recovery looks good, decide whether to `cp fpds_raw_v2/*.json fpds_raw/` (merging into the canonical dir) or keep parallel.
3. Re-run `pull_usaspending_subawards.py --discover` and `pull_sam_subawards.py` with the expanded PIID universe. NEW PIIDs from the recovered BIW/RR data will go through subaward pulls for the first time. THIS is what will materially expand "% of DDG cost" coverage.
4. Re-run `aggregate_sam_subawards.py` and the `nc_*` aggregations to refresh extracted/ CSVs.
5. (Optional) Re-score transcript "DDG relevance" with the narrow keyword subset, not the noisy one, to fix the agent's ranking.
6. (Optional) NAICS resolution for the 53 not-found UEIs via alternative sources.

### Known issues / decisions to revisit
- The 6 NAICS not-found UEIs are 35% of top-150 vendors — material gap.
- Transcript scraper for BAE only got 3 docs (2024-2025). Pre-2024 will need 20-F SEC filings or BAE IR site if needed.
- The Raytheon ESSM PIID `N0002415C5420` is worth a manual look — the Thales record may legitimately be a DDG component (ESSM ships ON destroyers) even if the $4.2B is the international cost-share. Decision needed on whether to keep, scale, or filter.

## How to resume

To continue this work in a fresh session, point next-Claude at:
1. This file (`logs/2026-05-24_session.md`)
2. `logs/transcripts_pull_summary.md`
3. `fpds_raw_v2/_summary.json` (when recovery completes)

Then re-run discovery + subaward pulls on the expanded PIID universe to
actually move the % needle.
